# Лиды

1 действие в домене `leads`.

| Команда | Описание | Параметры |
|---------|----------|-----------|
| `leads-get` | Получить лиды из форм лидогенерации | `SelectionCriteria` (dict) \*, `FieldNames` (list[str]) \*, `Page` (dict) |

\* — обязательный параметр
