# Креативы

2 действия в домене `creatives`.

| Команда | Описание | Параметры |
|---------|----------|-----------|
| `creatives-get` | Получить креативы для медийных объявлений | `SelectionCriteria` (dict) \*, `FieldNames` (list[str]) \*, `Page` (dict) |
| `creatives-add` | Создать креативы для медийных объявлений | `Creatives` (list[dict]) \* |

\* — обязательный параметр
