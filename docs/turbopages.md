# Турбо-страницы

1 действие в домене `turbopages`.

| Команда | Описание | Параметры |
|---------|----------|-----------|
| `turbopages-get` | Получить Турбо-страницы | `SelectionCriteria` (dict) \*, `FieldNames` (list[str]) \*, `Page` (dict) |

\* — обязательный параметр
